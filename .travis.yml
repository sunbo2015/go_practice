language: go
sudo: required
go:
  - 1.11.x

addons:
  apt:
    update: true

script:
  chmod +x build.sh && ./build.sh

# after_success:
# deploy:
#   provider: releases
#   api_key: ${GH_TOKEN}
#   file:
#     - libv2ray.aar
#   skip_cleanup: true
#   on:
#     tags: true
